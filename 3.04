public class Test
{
    static int countFives(int [] array)
    {
    int sum = 0;
        for(int i = 0; i < array.length; i++)
        {
            if(array[i] == 5)
                sum++;
            
        }
    return sum;
    }
}
